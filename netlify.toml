[build]
  functions = "functions"
  publish = "build"
  command = "npm install && npm run build"

[dev]
  functions = "functions"
  publish = "build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
